---
layout: default
title: A hint of chaos
---

- var posts = getFilesAtPath("posts").toJSON().sort(function(p1, p2) {
-   return p2.date.getTime() - p1.date.getTime()
- }).filter(function(p){ return !p.draft });

each post in posts.slice(0,5)
    .post
        h1
            a(href=post.url)=post.title
        .date= post.date.toDateString()
        .content!=post.contentRenderedWithoutLayouts

- var older = posts.slice(5);
if (older.length)
    hr
    h1 Older posts
    each post in posts.slice(5)
        h4.post
            a(href=post.url)=post.title
        
